{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "program": "${fileDirname}/main",
            "type": "lldb",
            "request": "launch",
            "stopOnEntry": true,
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "gcc make"
        }
    ]
}